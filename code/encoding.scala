import encoding.macros._

/* class StringEncodingContext(val sc: StringContext) {
  def utf8(s: String) = StringEncodingMacros.staticEncoding(UTF8, s)
}

object StringEncodingContext {
  implicit def yolo(sc: StringContext) = StringEncodingContext(s)
} */
